# Ignore everything in home directory by default
*
.*

# Track only the website project
!auroraflow-website/
!auroraflow-website/**
!README.md
!.gitignore
!netlify.toml

# Track design audit deliverables (repo root, never served)
!DESIGN-AUDIT-JOURNAL-BIOS-SERVICES.md
!DESIGN-AUDIT-JOURNAL-BIOS-SERVICES-MOCKUP.html

# Track Claude Code skills so they persist across sessions
!.claude/
!.claude/**
